/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  compiler: {
    styledComponents: true, // Styled Components uchun kompilyatsiya yoqiladi
  },
};

export default nextConfig;
